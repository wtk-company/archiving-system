@model IEnumerable<ArchiveProject2019.Models.Document>

@{
    ViewBag.Title = "وثائقي";
    Layout = "~/Views/Shared/_DashBoardLayout.cshtml";
}


@Html.Partial("_StartView")


@if (ViewBag.Msg != null)
{


    if (ViewBag.Msg == "PermissionError")
    {
        <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            لا يمكن القيام بالعملية لعدم القدرة على ذلك

        </div>
    }
    if (ViewBag.Msg == "CreateError")
    {
        <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            لا يمكن أن يكون الاسم مكرر، يجب إعادة الإدخال

        </div>
    }


    if (ViewBag.Msg == "CreateSuccess")
    {
        <div class="alert alert-info enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            تمت عملية الإضافة بنجاح
        </div>
    }


    if (ViewBag.Msg == "EditSuccess")
    {
        <div class="alert alert-success enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            تمت عملية تعديل البيانات بنجاح
        </div>
    }

    if (ViewBag.Msg == "EditError")
    {
        <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            لا يمكن أن يكون الاسم مكرر، يجب إعادة الإدخال

        </div>
    }


    if (ViewBag.Msg == "DeleteError")
    {
        <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            لايمكن القيام بعمليةالحذف، العمليةغير مسموحة

        </div>
    }
    if (ViewBag.Msg == "DeleteSuccess")
    {
        <div class="alert alert-danger enterRemove" style="text-align:center;font-size:larger">
            <span class="glyphicon glyphicon-bell">

            </span>
            تمت عملية الحذف بنجاح


        </div>
    }



}

<p>
    <button id="btnNewModel" class="btn btn-primary">إضافةوثيقة جديدة</button>
    <a href="@Url.Action("Search")" class="btn btn-success">بحث متقدم</a>
    <button id="btnRefreshModel" class="btn btn-default glyphicon glyphicon-refresh"></button>

</p>
<table id="form" class="table table-striped  table-hover dt" style="text-align:center">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FormId)
            </th>


            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayName("العمليات المتاحة")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Form.Name)
                </td>


                <td>
                    @Html.DisplayFor(modelItem => item.Subject)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.CreatedAt)
                </td>
                <td>


                    <div class="btn-group" role="group">
                        <button type="button" class="btn btn-sm btn-warning dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            الوثائق المرتبطة
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">

                            <li>

                                <a href="#" data-form-id="@item.Id" title="ربط وثيقة" class="btn-link js-relate btn-sm">ربط وثيقة جديدة</a>

                            </li>
                            <li>

                                <a href="#" data-form-id="@item.Id" title="اضافة وثيقة رد" class="btn-link js-replay btn-sm">اضافة وثيقة رد</a>

                            </li>

                            <li>
                                <a href="#" data-form-id="@item.Id" data-Related-id="@item.Id" title="تحرير وثائق الرد" class="js-releaseDocument">تحرير الرد</a>
                            </li>

                            <li>

                                <a href="@Url.Action("GetRelatedDocument","Documents",new { id=item.Id})" title="عرض الوثائق المرتبطة" class="btn-link  btn-sm">عرض الوثائق المرتبطة</a>

                            </li>
                            <li>

                                <a href="@Url.Action("GetReplayDocument","Documents",new { id=item.Id})" title="عرض الرد" class="btn-link  btn-sm">عرض الوثائق الرد</a>

                            </li>

                            <li>

                                <a href="@Url.Action("Relate","Documents",new { id=item.Id})" title="ربط وثائق" class="btn-link  btn-sm">ربط مع وثائق</a>

                            </li>


                        </ul>
                    </div>


                    <div class="btn-group" role="group">
                        <button type="button" class="btn btn-sm btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            خيارات إضافية
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu">

                            <li>

                                <a href="@Url.Action("Edit","Documents",new { id=item.Id})" title="تعديل الوثيقة" class="btn-link  btn-xs">تعديل الوثيقة</a>

                            </li>
                            <li>

                                <a href="@Url.Action("Details","Documents",new { id=item.Id})" title="تفاصيل الوثيقة" class="btn-link js-Details  btn-xs">تفاصيل الوثيقة</a>

                            </li>
                            <li>

                                <a href="@Url.Action("Create","DocumentDepartments",new { id=item.Id})" title="إضافة الاقسام للوثيقة" class="btn-link btn-sm">إضافة اقسام للوثيقة</a>

                            </li>
                            <li>

                                <a href="@Url.Action("Index","DocumentDepartments",new { id=item.Id})" title="الاقسام الخاصة بالوثيقة" class="btn-link  btn-sm"> الأقسام الخاصة بالوثيقة </a>

                            </li>

                            <li>

                                <a href="@Url.Action("Create","DocumentGroups",new { id=item.Id})" title="إضافة المجموعات للوثيقة" class="btn-link btn-sm">إضافة مجموعات للوثيقة</a>

                            </li>

                            <li>

                                <a href="@Url.Action("Index","DocumentGroups",new { id=item.Id})" title="المجموعات الخاصة بالوثيقة" class="btn-link  btn-sm"> المجموعات الخاصة بالوثيقة </a>

                            </li>
                            @if (item.TypeMail.Type == 3)
                            {
                                <li>
                                    <a href="@Url.Action("Create","DocumentTargetGroups",new { id=item.Id})" title="إضافة مجموعات مستهدفة" class="btn-link btn-sm">إضافة مجموعات مستهدفة</a>
                                </li>
                                <li>
                                    <a href="@Url.Action("Index","DocumentTargetGroups",new { id=item.Id})" title="المجموعات المستهدفة" class="btn-link  btn-sm"> المجموعات المستهدفة </a>
                                </li>
                                <li>
                                    <a href="@Url.Action("Create","DocumentTargetDepartments",new { id=item.Id})" title="إضافة اقسام مستهدفة" class="btn-link btn-sm">إضافة اقسام مستهدفة</a>
                                </li>
                                <li>
                                    <a href="@Url.Action("Index","DocumentTargetDepartments",new { id=item.Id})" title="الاقسام المستهدفة" class="btn-link  btn-sm"> الاقسام المستهدفة </a>
                                </li>
                            }
                            else if (item.TypeMail.Type == 2)
                            {
                                <li>
                                    <a href="@Url.Action("Create","DocumentParties",new { id=item.Id})" title="إضافة جهات مستهدفة" class="btn-link btn-sm">إضافة جهات مستهدفة</a>
                                </li>
                                <li>
                                    <a href="@Url.Action("Index","DocumentParties",new { id=item.Id})" title="الجهات المستهدفة" class="btn-link  btn-sm"> الجهات المستهدفة </a>
                                </li>
                            }
                        </ul>
                    </div>


                </td>
            </tr>
        }
    </tbody>


</table>

<div id="SearchResult"></div>

@Html.Partial("_LastView")


<div class="modal fade" tabindex="-1" id="NewModal"
     data-keyboard="true" data-backdrop="static">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">×</button>
                <h4 id="changeText" class="modal-title"></h4>
            </div>
            <div class="modal-body" id="partialPlaceHolder" style="margin-top:30px">

            </div>

        </div>
    </div>
</div>

<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script>

    $("#btnNewModel").on("click", function () {

        $("#changeText").text("إختيار نموذج معين");
        var url = '@Url.Action("form", "Documents")'

        $("#partialPlaceHolder").load(url, function () {
            $('#NewModal').modal('show');
        });
    });

    $("#form").on("click", ".js-relate", function () {

        $("#changeText").text(" إختيار نموذج معين");

        var url = '@Url.Action("RelateDocument", "Documents")'
        url = url + '/' + $(this).attr("data-form-id");

        $("#partialPlaceHolder").load(url, function () {
            $('#NewModal').modal('show');
        });
    });

    $("#form").on("click", ".js-replay", function () {

        $("#changeText").text(" إختيار نموذج معين");

        var url = '@Url.Action("ReplayDocument", "Documents")'
        url = url + '/' + $(this).attr("data-form-id");

        $("#partialPlaceHolder").load(url, function () {
            $('#NewModal').modal('show');
        });
    });

    $("#form").on("click", ".js-release", function () {
        $.ajax({
            type: "POST",
            url: "/Documents/Release",
            contentType: "application/json; charset=utf-8",
            data: '{"id":"' + $(this).attr("data-form-id") + '","id2":"' + $(this).attr("data-Related-id") + '"}',
            dataType: "html",
            success: function (result, status, xhr) {
                document.location.reload();
            },

        });
    });

    $("#form").on("click", ".js-releaseDocument", function () {
        $.ajax({
            type: "POST",
            url: "/Documents/ReleaseDocument",
            contentType: "application/json; charset=utf-8",
            data: '{"id":"' + $(this).attr("data-form-id") + '","id2":"' + $(this).attr("data-Related-id") + '"}',
            dataType: "html",
            success: function (result, status, xhr) {
                document.location.reload();
            },

        });
    });

    $("#btnRefreshModel").on("click", function () {
        window.location.replace('@Url.Action("Index","Documents",new { Id="none"})');
    });
</script>








@*@model IEnumerable<ArchiveProject2019.Models.RelatedDocument>

    @{
        ViewBag.Title = "الوثائق المرتبطة";
        Layout = "~/Views/Shared/_DashBoardLayout.cshtml";
    }


    @Html.Partial("_StartView")


    @if (ViewBag.Msg != null)
    {


        if (ViewBag.Msg == "PermissionError")
        {
            <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                لا يمكن القيام بالعملية لعدم القدرة على ذلك

            </div>
        }
        if (ViewBag.Msg == "CreateError")
        {
            <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                لا يمكن أن يكون الاسم مكرر، يجب إعادة الإدخال

            </div>
        }


        if (ViewBag.Msg == "CreateSuccess")
        {
            <div class="alert alert-info enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                تمت عملية الإضافة بنجاح
            </div>
        }


        if (ViewBag.Msg == "EditSuccess")
        {
            <div class="alert alert-success enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                تمت عملية تعديل البيانات بنجاح
            </div>
        }

        if (ViewBag.Msg == "EditError")
        {
            <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                لا يمكن أن يكون الاسم مكرر، يجب إعادة الإدخال

            </div>
        }


        if (ViewBag.Msg == "DeleteError")
        {
            <div class="alert alert-warning enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                لايمكن القيام بعمليةالحذف، العمليةغير مسموحة

            </div>
        }
        if (ViewBag.Msg == "DeleteSuccess")
        {
            <div class="alert alert-danger enterRemove" style="text-align:center;font-size:larger">
                <span class="glyphicon glyphicon-bell">

                </span>
                تمت عملية الحذف بنجاح


            </div>
        }



    }

    <a href="@Url.Action("index","Documents")" class="btn btn-default"> قائمة الوثائق  <i class="glyphicon glyphicon-backward"></i></a>

    <table id="form" class="table table-striped table-bordered table-hover dt" style="text-align:center">
        <thead>
            <tr>
                <th>
                    النموذج
                </th>
                <th>
                    القسم
                </th>

                <th>
                    الموضوع
                </th>
                <th>
                    العمليات المتاحة
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Document.Form.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Document.Department.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Document.Subject)
                    </td>
                    <td>
                        <a href="@Url.Action("Edit","Documents",new { id=item.Document.Id})" title="تعديل" class="btn-link  btn-xs  glyphicon glyphicon-edit"></a> |
                        <a href="#" data-form-id="@item.Id" data-Related-id="@item.Document.Id" title="تحرير ارتباط" class="js-release glyphicon glyphicon-remove-sign"></a> |
                        <a href="#" data-form-id="@item.Id" data-Related-id="@item.Document.Id" title="فصل الوثيقة المرتبطة" class="js-releaseDocument glyphicon glyphicon-remove-sign"></a> |
                        <a href="@Url.Action("GetReplayDocument","Documents",new { id=item.Document.Id})" title="عرض الرد" class="btn-link js-Details  btn-xs  glyphicon glyphicon-folder-open"></a> |
                        <a href="#" data-form-id="@item.Document.Id" title="ربط وثيقة" class="js-relate glyphicon glyphicon-share"></a> |
                        <a href="@Url.Action("GetRelatedDocument","Documents",new { id=item.Document.Id})" title="عرض الوثائق المرتبطة" class="btn-link js-Details  btn-xs  glyphicon glyphicon-folder-open"></a> |
                        <a href="@Url.Action("Details","Documents",new { id=item.Document.Id})" title="تفاصيل" class="btn-link js-Details  btn-xs  glyphicon glyphicon-book"></a>
                    </td>
                </tr>
            }
        </tbody>


    </table>

    <div id="SearchResult"></div>

    @Html.Partial("_LastView")




    <div class="modal" tabindex="-1" id="NewModal" sdata-keyboard="false" data-backdrop="static">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">×</button>
                    <h4 id="changeText" class="modal-title"></h4>
                </div>
                <div class="modal-body" id="partialPlaceHolder">

                </div>
            </div>
        </div>
    </div>

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script>

        $("#btnNewModel").on("click", function () {

            $("#changeText").text("القائمة الفرعية- إختيار نموذج معين");
            var url = '@Url.Action("form", "Documents")'

            $("#partialPlaceHolder").load(url, function () {
                $('#NewModal').modal('show');
            });
        });

        $("#form").on("click", ".js-relate", function () {

            $("#changeText").text("القائمة الفرعية- إختيار نموذج معين");

            var url = '@Url.Action("RelateDocument", "Documents")'
            url = url + '/' + $(this).attr("data-form-id");

            $("#partialPlaceHolder").load(url, function () {
                $('#NewModal').modal('show');
            });
        });


        $("#form").on("click", ".js-release", function () {
            $.ajax({
                type: "POST",
                url: "/Documents/Release",
                contentType: "application/json; charset=utf-8",
                data: '{"id":"' + $(this).attr("data-form-id") + '","id2":"' + $(this).attr("data-Related-id") + '"}',
                dataType: "html",
                success: function (result, status, xhr) {
                    document.location.reload();
                },

            });
        });

        $("#form").on("click", ".js-releaseDocument", function () {
            $.ajax({
                type: "POST",
                url: "/Documents/ReleaseDocument",
                contentType: "application/json; charset=utf-8",
                data: '{"id":"' + $(this).attr("data-form-id") + '","id2":"' + $(this).attr("data-Related-id") + '"}',
                dataType: "html",
                success: function (result, status, xhr) {
                    document.location.reload();
                },

            });
        });

        $("#btnRefreshModel").on("click", function () {
            window.location.replace('@Url.Action("Index","Documents",new { Id="none"})');
        });
    </script>*@
