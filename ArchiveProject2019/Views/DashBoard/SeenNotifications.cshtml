@model IEnumerable<ArchiveProject2019.Models.Notification>
@{
    /**/

    ViewBag.Title = "الإشعارات  المقروءة";
    Layout = "~/Views/Shared/_DashBoardLayout.cshtml";
}



@Html.Partial("_StartView")


@using (Html.BeginForm())
{

    <div id="NotId">

        <a href="@Url.Action("SeenNotifications")" id="btnRefreshModel" title="تحديث الصفحة" class="btn btn-default glyphicon glyphicon-refresh"></a>


        @if (Model.Count() > 0)
        {

            <input type="submit" value="تحديد الإشعارات المحددة للحذف" class="btn btn-primary" />

            <a class="btn btn-danger" href="@Url.Action("DeleteAllSeen")" title="حذف جميع الإشعارات">حذف جميع الإشعارات </a>

            <br />
            <br />
            foreach (var v in Model)
            {


                @Html.Hidden("Id", v.Id)
                <div style="opacity:0.8" class="alert alert-info enterRemove alert-dismissible" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <input style="width:20px;height:20px" type="checkbox" name="NotificationId" class="btn btn-default btn-xs" value="@v.Id" />
                    <strong><i>@(v.NotificationOwner.FullName + " في الوقت :" + v.CreatedAt)</i></strong>
                    <p style="font-size:16px;color:black">

                        @v.Message
                    </p>
                </div>


            }


        }
        else
        {
            <p style="text-align:center;padding:20px;">لا يوجد أي إشعارات  مقروءة</p>
        }

    </div>

}
@Html.Partial("_LastView")


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>

        $(document).ready(function () {


            $(".alert").mouseenter(function () {

                $(this).removeClass("alert-info");
                $(this).addClass("alert-danger");
            });


            $(".alert").mouseleave(function () {

                $(this).removeClass("alert-danger");
                $(this).addClass("alert-info");
            });


        });
    </script>
}



